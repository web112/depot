<h1><%= t('.editorder') %></h1>
<div class = "depot_form">
	<fieldset>
		<legend>
			<%= t('.enterorder') %>
		</legend>
<%= render 'form_edit' %>

<%= link_to t('.show'), @order %> |
<%= link_to t('.back'), orders_path %>
	</fieldset>
</div>