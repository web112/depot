<%= form_for(@book_type) do |f| %>
  <% if @book_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_type.errors.count, "error") %> prohibited this book_type from being saved:</h2>

      <ul>
      <% @book_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, t('.type_name')+":" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit')%>
  </div>
<% end %>
