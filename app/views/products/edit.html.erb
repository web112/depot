<h1><%=t('.editproduct')%></h1>
<div class = "depot_form">
	<fieldset>
		<legend>
			<%=t('.legend')%>
		</legend>
		<%= render 'form' %>

		<%= link_to t('.show'), @product %> |
		<%= link_to t('.back'), products_url %>
	</fieldset>
</div>